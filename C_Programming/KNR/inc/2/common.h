#include <stdio.h>
#include <stdlib.h>

//10. Converts Uppercase Characters to Lowercase

int lower(int);

//9. Counts 1's Bits in the Number

void bitcount(unsigned int, int *);

//8. Returns value of Integer x rotated to right to n bit positions

void rightrot(unsigned int *, int);

//7. Invert Bits that begin at Postition p

void invert(unsigned int *, int, int);

//6. Setbits bits from position p of x as per n rightmost bits of y

unsigned int setbits(unsigned int, int, int, unsigned int);

//4. Deletes each character in S1 that matches any character in S2

void squeeze(char *, char *);
